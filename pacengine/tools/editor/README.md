# PacEngine Editor (placeholder)

The editor tool will live here. It is intentionally empty in this scaffold.

Planned responsibilities:

- Author and validate PacData documents (`*.pacdata.json`).
- Inspect recorded traces produced by `engine/trace`.
- Drive `LocalWorker` jobs against author-time PacData for quick playback.

Until then, hand-author PacData under `examples/` and run it through
`pacengine_game` or the determinism test.
